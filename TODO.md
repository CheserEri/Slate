# Slate 项目待办事项

## 高优先级
- [x] 修复文件传输功能（任务创建后进度一直是0）
- [x] 在传输队列里添加取消功能

## 中优先级
- [x] 优化添加SMB文件夹时点击分享按钮弹出的子菜单颜色过浅问题

---

## 已完成

### 1. 修复 SMB 相册浏览 bug
- 问题：新添加的 SMB 服务器 ID 为空字符串，导致无法正确查找服务器
- 解决：使用时间戳生成唯一 ID，确保每个服务器都有唯一标识

### 2. 添加缓存持久化功能
- 创建 `CacheService` 类，支持：
  - 相册列表缓存
  - 媒体文件列表缓存
  - 图片文件缓存
  - 自动清理过期缓存（默认7天）

### 3. 添加缩略图加载功能
- 使用 `image` 库进行图片处理
- 生成 200x200 大小的缩略图，70% JPEG 质量
- 缩略图独立缓存，大幅提升加载速度
- 更新 `SmbRemoteImage` 组件支持使用缩略图

### 4. 实现无限滚动加载
- 添加分页加载支持，每页加载 50 条数据
- 滚动到底部自动加载更多
- 显示加载进度指示器

### 5. 优化多线程图片加载
- 实现 `_ImageLoadingController` 控制并发加载
- 限制最大并发数为 4，避免网络过载
- 使用队列机制管理待加载请求
